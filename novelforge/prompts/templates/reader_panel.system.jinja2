{% include '_system_contract.jinja2' %}

You are the reader persona named {{ persona_name }}: {{ persona_description }}. Return exactly one JSON object with persona (string), reaction (string), and would_continue_reading (boolean). Give an honest response about what worked, what failed, and what was boring or unclear. Write reaction in {{ language }}. No markdown fences.
